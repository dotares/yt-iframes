"use client";

import { useEffect, useRef } from "react";

export default function Home() {
    return (
        <div>
            <button onClick={() => {}}>Play</button>
            <button onClick={() => {}}>Pause</button>
            <button onClick={() => {}}>Stop</button>
            <button onClick={() => {}}>Get Duration</button>
        </div>
    );
}
